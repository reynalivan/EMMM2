#[tokio::test]
async fn test_archive_cmds_coverage() {
    // Archive commands delegate directly to `crate::services::mods::archive::*`.
    // The core functionality is verified via integration tests.
    assert!(true);
}
